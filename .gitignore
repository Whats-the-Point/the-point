# Swap Files
*.swp
*.swo

# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Dialyzer PLT files
/priv/plts/*.plt
/priv/plts/*.plt.hash

# structure sqls from mix ecto.dump
/priv/repos/*/structure.sql

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
the_point-*.tar

# If NPM crashes, it generates a log, let's ignore it too.
npm-debug.log

# Since we are building assets from assets/,
# we ignore priv/static. You may want to comment
# this depending on your deployment strategy.
/priv/static/

# Ignore VSCode files/folders
.elixir_ls/
.vscode/*
!.vscode/.code-snippets.json
!.vscode/settings.json

# Ignore .DS_Store
.DS_Store

/config/dev.secret.exs
/config/test.secret.exs

.env
.env.dev

# Ignore local uploads
/media/*

# Ignore pem files
*.pem
*.rdb

# Ignore Intellij files/folders
.idea/
the_point.iml

# Temporary files
tmp

# we don't want to commit generated markdown docs
docs/generated/*.md

# to temporarily remove a test CSV. Removed after the script is deleted
kyb_flag_changes.csv
fraud_payments_report.csv

# ignore the logfile
logfile

# node
node_modules